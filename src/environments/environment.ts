// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  apiDevHost: '',
  apiSuraHost: 'https://api.us-south.apiconnect.appdomain.cloud/surape-integradesaappconsulta360/app-consulta-360/api/private',
  baseHref: ''
};


/*
 *
 *apiSuraHost: 'http://lbd-consulta-360-core-acc2e6f6ae567b7a.elb.us-west-2.amazonaws.com/consulta-360-core-service/api',
 * 
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
