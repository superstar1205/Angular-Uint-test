export interface SearchResponse {
    statusResponse? : boolean,
    statusResponseCode? : number,
    statusResponseMessage? : string,
    countBody? : number,
    statusResponseBody? : any,
    responseErrors? : any,
}